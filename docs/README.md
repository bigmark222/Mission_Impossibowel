# Module docs

- `vision/`: detector, capture interfaces, overlay helpers (see also tests/vision_overlay.rs).
- `sim/`: autopilot/recorder modules for sim/datagen modes.
- `tools/`: CLI utilities (overlay_labels, prune_empty, datagen_scheduler, warehouse_etl/export/cmd). See docs/book/reference for usage.
